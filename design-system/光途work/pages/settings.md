# Settings / Skills / MCP overrides

Master 浅色令牌全部生效。

## 技能 / 连接列表

- 每张卡片左侧 40×40 圆角图标，固定 viewBox 尺寸，不用 emoji
- 内置目录有 Lucide 图标；三方或自定义没有自带图标时，用名称第一个字
- 图标底色由 id 哈希到有限色板，对比度足够
- Hover 只改边框/背景色，200ms，不 scale

## 设置页

- 分区卡片：账户、模型与网关、工作空间与权限、应用
- 每个输入有可见 label，不用 placeholder 当唯一说明
- 分区标题带 Lucide 图标 + 简短说明
- 检查更新要有成功/失败文案，不只改按钮颜色
