# Studio / 创作 overrides

Master 浅色令牌全部生效。本页：

- 侧栏「创作」与「任务」并列，不要塞进对话框当一种 Agent 模式
- 图片 / 视频用分段控件切换，选中主色填充，无 hover scale
- 左表单右作品墙：描述、模型、清晰度 / 时长、可选参考图；预计消耗只显示套餐百分比
- 生成中用「思考中」圆点，视频允许排队 1–3 分钟
- 作品卡白底圆角 + `shadow-sm`，图片内嵌预览，视频优先播放远程地址，本机文件用打开 / 位置
- 体验版不含生图生视频，空态引导升级；未登录去账户页
- 厂商 Key 仍只在内部控制台「供应商」配置，客户端不出现 sk- 输入框
